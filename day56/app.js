// 3. The postfix and prefix forms:
   
let a = 1, b = 1;

let c = ++a; // Now a = 2, c = 2 (prefix form increments a before using its value)
let d = b++; // Now b = 2, d = 1 (postfix form increments b after using its value)

console.log("value of c:" , c);
console.log("value of d:" , d);