// Question 56: Keep Only Strings: Given a mix of different types of items, make a new list that has only the words.

function findMaximumNumber(Numberumbers: number[]): number {
    Numberumbers.sort((a, b) => b - a);
    return Numbers[0];
}

const Numbers: number[] = [10, 25, 7, 42, 15];
const maximumNumber: number = findMaximumNumber(Numbers);
console.log("Maximum Number:", maximumNumber);