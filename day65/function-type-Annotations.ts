
//  FUNCTION TYPES & ANNOTATIONS  
// Rectangle area calculation function
const calculateRectangleArea = function(length: number, width: number): number {
    return length * width;
  }
  
  // Function call with valid arguments
  const lengthRectAngle = 4;
  const widthofRectAngle = 4;
  const area = calculateRectangleArea(lengthRectAngle, widthofRectAngle);
  console.log(`The area of the rectangle is: ${area}`);
  